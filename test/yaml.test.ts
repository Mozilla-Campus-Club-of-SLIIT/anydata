import assert from "assert"

// Placeholder suite. Real YAML support is not implemented yet, so we simply
// keep a green test to verify the harness runs.
describe("yaml", () => {
  it("should have some tests", async () => {
    assert.ok(true)
  })
})
